<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WowrackCustomerApp"
        tools:targetApi="31">
        <activity
            android:name=".ui.main.section.profile.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".ui.main.section.hotspot.WownetHotspot"
            android:exported="false" />
        <activity
            android:name=".ui.main.section.detail.DetailTraffic"
            android:exported="false" />
        <activity
            android:name=".ui.main.section.detail.DetailArticle"
            android:exported="false" />
        <activity
            android:name=".ui.main.section.home.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".ui.main.section.modifypackage.ConfirmModifyActivity"
            android:exported="false" />
        <activity
            android:name=".ui.main.section.modifypackage.ModifyPackageActivity"
            android:exported="false"
            android:theme="@style/WowrackCustomerApp"
            android:windowSoftInputMode="adjustResize" />
        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBXGeDYABoPmg2zk8Gotqml1BXNAapD1a4" />

        <activity
            android:name=".ui.main.section.help.HelpActivity"
            android:exported="false"
            android:theme="@style/WowrackCustomerApp"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:theme="@style/WowrackCustomerApp"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false"
            android:theme="@style/WowrackCustomerApp"
            android:windowSoftInputMode="adjustResize" />

        <service
            android:name=".data.firebase.MessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>